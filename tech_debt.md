Протокол разработки:

1. Старайся писать на класса с принципами ООП. -- уже есть, просто поддерживай но без фанатизма.
2. Конфиги, при инициализации читай напрямую при помощи  [''] а не get(..., default)
3. Также проследи за тем чтобы в коде не было магических переменных и констант -- все выносим на свет Божий в конфиги.
4. Каждый файл -- новые и старые помечаем шапкой вверху с именем-путем файла (путь относительно проекта) и коротко ролью.

~~=== v7 ===~~ [COMPLETED]

Заменяем архитетктуру топорного входа по маркету в две ноги на гибкий алгоритм LIMIT_IOC. Алгоритм Самсонова. Это моя авторская методика, скорее всего "велосипед", но надеюсь двухпедальный и рабочий.

* **СТАТУС ИНВАРИАНТОВ (ОТЧЕТ):**
  * **Архитектура LIMIT_IOC внедрена:** `PositionFSM` полностью переведен на Фазы 1-4 (асимметричный вход Lead -> Hedge).
  * **Защита minNotional:** Валидируется на этапе 2 (до подтягивания второй ноги).
  * **Model Drift Ratio:** Отклонение от расчетной цены жестко контролируется. Если $> 80\%$, нога немедленно сбрасывается маркетом (Kill-Switch) и уходит в карантин.
  * **Динамическая подрезка излишка ($\Delta = 0$):** Если недолив Hedge-ноги составляет менее 25%, излишек Lead-ноги аккуратно срезается MARKET-ордером `reduce_only`.
  * **Баг Bitget устранен/изолирован:** Обнаружена ошибка API Bitget `22002 No position to close` при попытке частичной подрезки в Hedge Mode. Коннектор был переработан: при возникновении этой ошибки FSM грациозно ловит `Exception`, логирует предупреждение и оставляет минимальную асимметрию, защищая систему от полного случайного закрытия всей ноги (что случалось ранее при жестком вызове `/close-positions`). Инвариант сохранения позиции выполнен.
  * **Код передан и закоммичен (Хэш `3c5ec09`).**

~~Устраивает ли такая структура конфигурации и порогов перед передачей агенту в ветку `v7`?~~

~~Как видишь, агент предложил вместо сенария A3 план с мгновенным подрезом ноги, а также валидацией минимального размера ордера для второй ноги -- что есть смарт мув.~~

~~выкатишь план, задашь вопросы и сразу приступишь к рефакторингу. Итеративные правки применим по ходу.~~






В конфигах бдут заданы по паре бирж слабая и сильная ноги (например Бинанс сильная Битгет слабая, Бинанс сильная Кукоин слабая). В момент поступления сигнала, кидаем LIMIT_IOC ордер на слабую ногу с жестко заданным порогом проскальзывания (для этой ноги он будет минимальным порядка 1.0005, т.е. -0.05%). Делаем это последовательно, через await. Дожидаемся await и парсим ответ. 

Если нога не налилась вовсе задействуем вариант Б -- завершаем торговую итерацию, монету вносим во временный карантин. Параметр времени нахождения в карантине для этого сценария строго декларируем в конфигах, оттуда же и извлекаем.

Если нога налилась -- на сколько бы то ни было по количеству -- задействуем ветку алогоритма A.

    A: Парсим точное количество и цену входа. Если проскальзывание от планируемой-бумажнорасчетной цены составило более 80% по этой ноге, то применяем вариант А1 -- быстро кроем ордер по маркету и заносим монету во временный карантин (по умолчанию на 1 час). Кстати, в этой связи, эти параметры надо будет задать несколько жестче:
            ("check_synthetic_exit": true,
            "check_synthetic_slippage": true,
            "max_slippage_ratio": 0.5,
            "hard_max_slippage": 0.015,).
   (Параметры карантина для всех вариаций должны быть четко задекларированы в конфигах (можно отдельным блоком, а не торчать магией в коде)).
   Если проскальзывание в рамках вышедозволенного то применяем вариант А2 -- подтягиваем вторую ногу. (на это у нас будет порядка пол секунды -- параметр будет прописан последним звеном карты деградации по входу по второй ноге). Сперва я думал бить вторую ногу маркетом (но пока отложим эту идею либо оставим по ней заглушку, в конфиге же оставим такой вариант в документации). Итак, основной вариант сценария А2 следующий: кидаем LIMIT_IOC на вторую ногу (в цикле), всего будет например три попытки, -- они будут прописаны по карте деградации входа во вторую ногу. карта деградации будет прописана индексами, дальностью заброса лимитной заявки (то есть допустимым проскальзыванием, а дальность лимитной заявки будет расчитана итеративно) и таймингом (например первая попытка в течение первых 50 мс). Проскальзывание будет заданно как рейт за вычетом (spread_entry минус фактическое проскальзывание первой ноги) * на этот самы заданный рейт, например 0.75 (и в зависимости от этого расчитываем множитель цены ордера LIMIT_IOC -- 1... для лонга и 0.9... для щорта). Также последовательно парсим await ответ от второй ноги, если биржа отклонила, то переходим к следующей ноге... выдерживаем допустимое проскальзывание, тайминг и так далее. В случае ближайшего успеха, переходим на третий этап машины состояний -- фиксируем фактический порог входа и переходим на карту деградации профита.
        Текущую карту деградации профита, которая завязана на чистой прибыли нужно перевести на относительный величины -- рейт от фактического спреда входа обеих ног.

   По истечение карты деградации входа по второй ноге, если так и не дали войти по допустимо-плановым ценам, а также при условии, что от фактического налива первой ноги, дали налиться по второй ноге хотя бы на 75% -- min_fill_rate -- параметр тоже задавать в конфигах, тогда переходим к варианту А3.

    A3: -- переходим на карту екстренного закрытия обеих ног -- extreme_profit_decay_map (если было в две ноги но min_fill_rate был меньше дозволенного) или

    A4: -- кроем по маркету первую ногу если во вторую ногу так и не налили.


Вот реплика внешнего агента:

### ТЗ: Асимметричный последовательный алгоритм входа LIMIT_IOC (Branch v7)

Вход в позицию переводится с параллельного `MARKET` на последовательный асимметричный протокол исполнения: Lead Leg (слабая биржа: Bitget/KuCoin) $\rightarrow$ Hedge Leg (сильная биржа: Binance).

---

### 1. Конфигурация (`cfg.json`)

В секцию `trading_rules.entry` добавляются параметры ролей бирж, лимитов проскальзывания и ступеней входа:

```json
{
  "trading_rules": {
    "entry": {
      "order_execution_type": "ASYMMETRIC_LIMIT_IOC",
      "exchange_roles": {
        "BINANCE_BITGET": {"lead": "BITGET", "hedge": "BINANCE"},
        "BINANCE_KUCOIN": {"lead": "KUCOIN", "hedge": "BINANCE"}
      },
      "lead_max_slippage_pct": 0.0005,
      "max_model_drift_ratio": 0.80,
      "min_hedge_fill_rate": 0.75,
      "hedge_decay_map": [
        {"iter": 0, "decay_rate": 0.50, "timeout_ms": 50},
        {"iter": 1, "decay_rate": 0.75, "timeout_ms": 150},
        {"iter": 2, "decay_rate": 1.00, "timeout_ms": 300}
      ],
      "quarantine_durations_sec": {
        "zero_fill": 300,
        "model_drift": 3600,
        "hedge_failed": 1800
      }
    },
    "exit": {
      "relative_profit_decay_map": [
        {"step": 0, "seconds": 0,   "ratio": 0.80},
        {"step": 1, "seconds": 15,  "ratio": 0.60},
        {"step": 2, "seconds": 45,  "ratio": 0.40},
        {"step": 3, "seconds": 120, "ratio": 0.20},
        {"step": 4, "seconds": 240, "ratio": 0.00}
      ]
    }
  }
}

```

---

### 2. Пошаговый сценарий конечного автомата (`PositionFSM`)

#### Фаза 1: Выстрел в Lead Leg (Слабая нога)

* По расчетному сигналу Numba Engine шлется одиночный `LIMIT_IOC` на слабую биржу:
* Long: $P_{\text{limit}} = P_{\text{ask}} \cdot (1 + \text{lead\_max\_slippage\_pct})$
* Short: $P_{\text{limit}} = P_{\text{bid}} \cdot (1 - \text{lead\_max\_slippage\_pct})$


* Выполняется `await` ответа матчинг-движка.
* **Ветка Б (Zero Fill):**
* Если `filled_qty == 0`: мгновенное прерывание торгового цикла, статус `ABORTED`.
* Монета отправляется в карантин на `quarantine_durations_sec.zero_fill` (5 мин). Финансовый результат: **0 комиссий, 0 потерь**.



---

#### Фаза 2: Валидация налива Lead Leg (Ветка А)

Из ответа и приватного сокета извлекаются точные фактические значения: $Q_{\text{lead}}$, $P_{\text{lead\_actual}}$.

* **Защита minNotional:**
* Проверка номинала: $\text{Notional}_{\text{usd}} = Q_{\text{lead}} \cdot P_{\text{lead\_actual}}$.
* Если $\text{Notional}_{\text{usd}} < \text{minNotional}_{\text{hedge}}$ (менее $5 на Binance): сброс Lead Leg по рынку, карантин `hedge_failed`, выход.


* **Ветка А1 (Фильтр отклонения от теоретической модели):**
* Расчет расхождения фактической цены налива относительно синтетической расчетной цены модели:

$$\text{Model Drift Ratio} = \frac{\vert{}P_{\text{lead\_actual}} - P_{\text{lead\_calc\_vwap}}\vert{}}{P_{\text{lead\_calc\_vwap}} \cdot \text{spread\_entry}}$$


* Если $\text{Model Drift Ratio} > \text{max\_model\_drift\_ratio}$ (более $80\%$ расчетного спреда съедено уже на первой ноге):
1. Немедленная ликвидация ноги Lead встречным `MARKET`-ордером.
2. Помещение монеты в карантин на `model_drift` (3600 сек).
3. Статус `ABORTED_DRIFT`.





---

#### Фаза 3: Дожим сильной ноги (Ветка А2)

Если отклонение в пределах нормы, запускается цикл подтягивания Hedge Leg (Binance) по `hedge_decay_map` (до 3 итераций):

* Для каждой попытки $i \in [0, 1, 2]$:
1. Вычисляется доступный буфер уступки спреда:

$$\text{Concession} = (\text{spread\_entry} - \text{drift}_{\text{lead}}) \cdot \text{decay\_rate}_i$$


2. Лимитная цена ордера рассчитывается от **актуального живого стакана Binance** ($P_{\text{hedge\_live}}$), гарантируя чистый спред:
* Если Hedge = BUY: $P_{\text{limit}} = P_{\text{hedge\_live\_ask}} \cdot (1 + \text{Concession})$
* Если Hedge = SELL: $P_{\text{limit}} = P_{\text{hedge\_live\_bid}} \cdot (1 - \text{Concession})$


3. Отправка `LIMIT_IOC` на объем $Q_{\text{needed}} = Q_{\text{lead\_actual}} - Q_{\text{hedge\_filled}}$.
4. Ожидание ответа с таймаутом `timeout_ms`.
5. Если нога налита на $100\%$: цикл прерывается, статус `ACTIVE_HEDGED`.
6. Если нога отклонена или налита частично: переход к следующей ступени карты.



---

#### Фаза 4: Разрешение остаточного объема (Инвариант $\Delta = 0$)

После завершения всех итераций карты деградации:

* **Ветка А3 (Частичный налив $\ge 75\%$):**
* Если $Q_{\text{hedge\_filled}} / Q_{\text{lead\_actual}} \ge \text{min\_hedge\_fill\_rate}$:
1. **Срез излишка Lead Leg:** В ту же миллисекунду отправляется встречный `MARKET` `reduceOnly=True` на слабую биржу на объем $\Delta Q = Q_{\text{lead\_actual}} - Q_{\text{hedge\_filled}}$.
2. Позиции выравниваются: $Q_{\text{lead}} = Q_{\text{hedge}}$. **Инвариант $\Delta = 0$ строго соблюден.**
3. Связка переводится на аварийную карту `extreme_profit_decay_map`.




* **Ветка А4 (Срыв налива $< 75\%$):**
* Если сильную ногу налить не удалось:
1. Полный сброс открытого объема Lead Leg по рынку.
2. Отмена оставшихся остатков на Hedge Leg.
3. Карантин на `hedge_failed` (1800 сек).





---

#### Фаза 5: Относительный выход (Relative Profit Decay)

* Целевой чистый спред выхода ($T_{\text{net}}$) рассчитывается динамически от зафиксированного спреда входа:

$$\text{Target Net Spread} = \text{Actual Net Spread}_{\text{entry}} \cdot \text{ratio}$$


* При падении рыночного спреда до $T_{\text{net}}$ — выход обеих ног встречными `LIMIT_IOC`.

---
