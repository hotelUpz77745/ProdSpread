Протокол разработки:

1. Старайся писать на класса с принципами ООП. -- уже есть, просто поддерживай но без фанатизма.
2. Конфиги, при инициализации читай напрямую при помощи  [''] а не get(..., default)
3. Также проследи за тем чтобы в коде не было магических переменных и констант -- все выносим на свет Божий в конфиги.
4. Каждый файл -- новые и старые помечаем шапкой вверху с именем-путем файла (путь относительно проекта) и коротко ролью.


1. Переводим систему из бумажной торговли в полноценного торгового бота.
2. Пока встраиваем и отлаживаем 2 адаптора открытия-закрытия ордеров -- для бирж Binance и Kucoin -- все фьючерсы. Но система должна быть легко расширяемой (фундамент уже заложен)

3. Управлением ордерами (открытие агрессивной лимиткой с последующей отменой остатков, закрытие (пока только по маркету)) должны совершаться чрез подключение к стриму потоков. Благо для обеих бирж уже есть апи вебсокетов для реализации этого механизма. Цель -- максимально ускорить скорость экзекьюции ордеров и максимально приблизить ее к скорости лучших HFT систем в данной индустрии. Это должны быть независимые, отказоустойчивые, пинговозобновляемые и тестируемые классы. Можно так и поместить в # FILE: API/orders.py а сами методы вызова ордеров оставить и превратить в компактный кулак интерфесов-оберток. В дальнейшем планируем добавить сюда и другие биржи.

4. Не знаю, обесечивают ли данного рода апи (стримы постановки ордеров) инструменты мониторинга позиции на предмет количества сайза открытых сделок и цены исполнения, если нет то нужно будет добавить классы приватных стримов на парсинг открытых позиций. Смотри как это рабочим образом уже реализовано для другого проекта для биржи Бинанс-Фьючерсы -- C:\Users\user\Desktop\My_Pro\HP_EliteBook_735_old\MY\FUNDING\ARB_FUNDING_BOT\API\BINANCE\ws_private.py +
C:\Users\user\Desktop\My_Pro\HP_EliteBook_735_old\MY\FUNDING\ARB_FUNDING_BOT\CORE\models_fsm.py

5. Конкретные моменты по коду:
execute_open после создания задач ордеров и паузы 
await asyncio.sleep(self.cfg["EXECUTION_PAUSE"]) нужно дергать метод отмены ордеров и дергать методы получения реальных размеров исполненности позиций (под обе ноги конкретной связки) и цен исполнения (по сути то, что пытается эмулировать код ниже). Роли кореллят long_executed_volume_rate и short_executed_volume_rate остаются в силе.